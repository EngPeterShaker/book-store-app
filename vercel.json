{
  "buildCommand": "cd apps/web && yarn build",
  "outputDirectory": "apps/web/build",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
