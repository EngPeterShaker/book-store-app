{
  "buildCommand": "yarn install && cd apps/web && yarn build && cd ../api && yarn build",
  "outputDirectory": "apps/web/build",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
