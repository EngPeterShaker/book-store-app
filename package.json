{
  "name": "book-store-app",
  "version": "1.0.0",
  "description": "Full-stack book store application with NestJS backend and React frontend",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "cd frontend && REACT_APP_API_URL=/api npm run build",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && yarn start:dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "keywords": [
    "nestjs",
    "react",
    "typescript",
    "postgresql",
    "vercel",
    "fullstack",
    "monorepo"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
